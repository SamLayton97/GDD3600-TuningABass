/// @description Used for initialization

// save dimensions of text box sprite
boxWidth = sprite_get_width(mySprite);
boxHeight = sprite_get_width(mySprite);

// save height of my message
stringHeight = string_height(myMessage);